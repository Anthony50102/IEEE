"""HW physics: fluxes, Poisson bracket, RHS, reference parameters.

Author: Anthony Poole

The canonical Gamma_n / Gamma_c implementations live here. Every method that
reports these QoIs must route through this module so that numerical results
are comparable across baselines.

The detailed implementations are being ported from `shared/physics.py` as
the refactor progresses; see that file in archive/ for the prior version.
"""

raise NotImplementedError("hw.physics: port from shared/physics.py during Phase 0")
