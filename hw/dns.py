"""HW DNS runner.

Author: Anthony Poole

Thin wrapper around the upstream `hw2d` reference solver. Reads a YAML config
and produces a single trajectory HDF5 file plus a sidecar `data_card.yaml`
recording the DNS parameters and time-averaged Gamma_n / Gamma_c.

Used in Phase 1 to generate the 256^2 trajectories at alpha in {0.1, 1, 5}
and the held-out alpha=1.5.
"""

raise NotImplementedError("hw.dns: implement Phase 1")
