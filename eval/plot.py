"""Plotting for the paper.

Author: Anthony Poole

Reads run artifacts (run_summary.yaml + npz) and emits figures. No
inference, no training. Ported incrementally from `shared/plotting.py`
(1980 LOC) -- only functions actually referenced by paper figures get
ported; the rest stays in archive/.
"""

raise NotImplementedError("eval.plot: port from shared/plotting.py during refactor")
